# Only VITE_* is exposed to the browser — never put secrets here.
# Production API (Render). For local Express, use e.g. http://localhost:4000 in `.env.local`.
VITE_API_URL=https://astra-m00i.onrender.com
